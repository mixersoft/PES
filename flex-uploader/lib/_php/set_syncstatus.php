<?php 
	//$desktop_id = $_POST['desktop_id'];
	//$uuid = $_POST['uuid']; //photo_id
	//$data = $_POST['json'];  //$data has changed columns which has to save in server db against requested $uuid
	//here your logic to save this data and return
	//always success as response
	//if any other string then client check fires failure function never 
	echo "success"; //internally client check "success" string to know whether data set by server successfully or not
?>